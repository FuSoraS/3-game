// Seleccionando el temporizador que es un parrafo
const timer = document.getElementById("timer");
// Agrega aleatoridad a las imagenes que aparecen
const numRandom = Math.floor(Math.random() * 3) + 1;

export {timer, numRandom};